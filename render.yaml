services:
  - type: web
    name: PatientSeg
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: PatientSeg
          property: connectionString

databases:
  - name: PatientSeg-db
    plan: free